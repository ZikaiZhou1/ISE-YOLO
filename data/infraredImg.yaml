path: ./datasets/infraredImgDataset  # dataset root dir
train: train/images  # train images (relative to 'path') 128 images
val: val/images  # val images (relative to 'path') 128 images
test: test/images # test images (optional)

# Classes
nc: 14 # number of classes
names: ['transformer', 'current transformer', 'oil conservator', 'insulator', 'post insulator', 'tube', 'switch',
        'clamp', 'circuit breaker', 'potential transformer', 'lightning arrester', 'wave trap', 'capacitor',
        'reactor']  # class names
